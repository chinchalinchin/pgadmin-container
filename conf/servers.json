{
    "Servers": {
        "1": {
            "Name": "$SERVER_NAME",
            "Group": "$GROUP_NAME",
            "Port": "$POSTGRES_PORT",
            "Username": "$POSTGRES_USER",
            "Host": "$POSTGRES_HOST",
            "SSLMode": "prefer",
            "MaintenanceDB": "postgres",
            "PassFile": "$PGPASSFILE"
        }
    }
}